{
  "description": "Enforces the operator-decision rule at the two boundaries where it is broken: a tracker write that hands over or files a decision without the whole record, and a turn that ends having handed the operator a decision in prose. Both are inert unless the project's own constitution declares an operator-decision section, so installing this plugin changes nothing in a project that has not adopted the rule.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/check-od-record.py\"",
            "timeout": 15
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/check-od-form.py\"",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
